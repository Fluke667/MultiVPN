{
    "server": "${SS_SERVER_ADDR}",
    "server_port": ${SS_SERVER_PORT},
    "local_address": "${SS_LOCAL_ADDR}",
    "local_port": ${SS_LOCAL_PORT},
    "password": "${SS_PASSWORD}",
    "timeout": ${SS_TIMEOUT},
    "method": "${SS_LOCAL_PORT}"
}
